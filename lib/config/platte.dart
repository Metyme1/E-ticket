import 'package:flutter/material.dart';

class platte extends StatelessWidget {
  const platte({Key? key}) : super(key: key);

  // Define colors here
  static const Color lightOrange = Color(0xFFFDE1C7);
  static const Color darkblue = Color(0xFF343A4A);
  static const Color lightGreen = Color.fromRGBO(144, 238, 144, 1.0);
  static const Color yellow = Color(0xFFFDE1C7);
   static const Color black = Color(0xFF2D2B2E);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}