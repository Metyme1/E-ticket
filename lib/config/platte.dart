import 'package:flutter/material.dart';

class platte extends StatelessWidget {
  const platte({Key? key}) : super(key: key);

  // Define colors here
  static const Color lightOrange = Color.fromRGBO(255, 200, 150, 1.0);
  static const Color darkBlue = Color.fromRGBO(0, 0, 50, 1.0);
  static const Color lightGreen = Color.fromRGBO(144, 238, 144, 1.0);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}