

import 'package:flutter/material.dart';

class SeatTypeConferencePage extends StatelessWidget {
  const SeatTypeConferencePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
